#! /bin/bash
set -e

if [ -n "$CONSUL_HOST" ]; then
  /usr/local/bin/consul agent \
    -join $CONSUL_HOST \
    -node $(hostname) \
    -data-dir /tmp/consul \
    -bind 0.0.0.0 \
    -config-dir /etc/consul.d >> /var/log/consul.log 2>&1
else
  # Wait forever input
  cat
fi
